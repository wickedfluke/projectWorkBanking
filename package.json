{
  "name": "projectworkbanking",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && concurrently \"npm --prefix frontend install\" \"npm --prefix backend install\"",
    "start": "concurrently \"npm run start-frontend\" \"npm run start-backend\"",
    "start-frontend": "cd \"Frontend\" && npm start",
    "start-backend": "cd \"Backend\" && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "projectworkbanking": "file:"
  }
}
